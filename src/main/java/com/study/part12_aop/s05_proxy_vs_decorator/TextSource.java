package com.study.part12_aop.s05_proxy_vs_decorator;

/**
 * [12.5] 데코레이터/프록시가 공통으로 감쌀 인터페이스.
 *
 * 12.5의 핵심 메시지는 "데코레이터 패턴과 프록시 패턴은 '구조(모양)'가 같고 '의도'만 다르다"는 것이다.
 * 둘 다: ①대상과 같은 인터페이스를 구현하고 ②대상(target)을 필드로 들고 ③호출을 가로채 위임한다.
 * 그래서 같은 TextSource 인터페이스로 두 패턴을 나란히 구현해 '모양은 같음'을 코드로 드러낸다.
 *  - 데코레이터(기능 추가): 결과를 꾸미거나 기능을 더한다(대문자화, 괄호 감싸기...). 여러 개 '쌓을' 수 있다.
 *  - 프록시(접근 제어): 대상에 접근할지 말지를 통제한다(권한·캐싱·지연 로딩).
 */
public interface TextSource {

    String read();
}
